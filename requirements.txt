# Pinned so the saved model unpickles identically in the cloud.
flask==3.1.3
gunicorn==23.0.0
scikit-learn==1.9.0
joblib==1.5.3
numpy==2.4.6
scipy==1.17.1
openai==2.43.0
python-dotenv==1.2.2

# Used only by train.py / download_data.py (not needed to serve the app)
pandas==3.0.3
matplotlib==3.10.7
